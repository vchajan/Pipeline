import { KeyRound } from "lucide-react";

export function LoginPage() {
  return (
    <section className="login-page">
      <KeyRound aria-hidden="true" />
      <h1>Login</h1>
      <p>Keycloak login wiring is scheduled for a later phase.</p>
    </section>
  );
}
